# MCP进阶

以创建一个查询天气的 MCP SERVER 为例



1 环境准备

python uv vscode/pycharm



2 创建项目

创建项目

```shell
uv init weather
```

创建虚拟环境

```shell
uv venv
```

激活虚拟环境

```shell
source .venv/bin/activate
```

windows

```shell
.venv\bin\activate
```



使用 pycharm 创建项目时，可以选择环境类型为 uv，会自动执行上面的步骤



2 安装依赖

```shell
uv add "mcp[cli]" httpx
```



3 创建 weather.py 文件

```python
import logging
from typing import Any
import httpx
from mcp.server.fastmcp import FastMCP

mcp = FastMCP("weather", log_level="ERROR")

NWS_API_BASE = "https://api.weather.gov"
USER_AGENT = "weather-app/1.0"

async def make_nws_request(url: str,)-> dict[str,Any]|None:
    """Make a request to the NWS API with proper error handling."""
    headers={
        "User-Agent": USER_AGENT,
        "Accept": "application/geo+json",
    }

    async with httpx.AsyncClient() as client:
        try:
            response = await client.get(url, headers=headers, timeout=30.0)
            response.raise_for_status()
            return response.json()
        except Exception:
            return None

def format_alert(feature: dict[str, Any]) -> str:
    """Format an alert feature into a readable string."""
    props = feature["properties"]
    return f"""
Event: {props.get('event', 'Unknown')}
Area: {props.get('areaDesc', 'Unknown')}
Severity: {props.get('severity', 'Unknown')}
Description: {props.get('description', 'No Description available')}
Instructions: {props.get('instructions', 'No specific instructions provided')}
"""

@mcp.tool()
async def get_alert(state: str) -> str:
    """Get weather alerts for a US state.

    Args:
        state: Two-letter US state code (e.g. CA, NY)
    """
    url = f"{NWS_API_BASE}/alerts/activate/area/{state}"
    data = await make_nws_request(url)

    if not data or "features" not in data:
        return "Unable to fetch alerts or no alerts found."

    if not data["features"]:
        return "No active alerts for this state."

    alerts = [format_alert(featrue) for featrue in data["features"]]
    return "\n---\n".join(alerts)


@mcp.tool()
async def get_forecast(latitude: float, longitude: float) -> str:
    """Get weather forecast for a location.

    Args:
         latitude: Latitude of the location
         longitude: Longitude of the location
    """

    points_url = f"{NWS_API_BASE}/points/{latitude},{longitude}"
    points_data = await make_nws_request(points_url)

    if not points_data:
        return "Unable to fetch forecast data for this location."

    forecast_url = points_data["properties"]["forecast"]
    forecast_data = await make_nws_request(forecast_url)

    periods = forecast_data["properties"]["periods"]
    forecasts=[]

    for period in periods[:5]:
        forecast = f"""
{period['name']}:
Temperature: {period['temperature']}°{period['temperatureUnit']}
Wind: {period['windSpeed']} {period['windDirection']}
Forecast: {period['detailedForecast']}
"""
        forecasts.append(forecast)

    return "\n---\n".join(forecasts)


if __name__ == "__main__":
    mcp.run(transport='stdio')
```

在Cline中添加配置，用来启动自定义的 weather MCP Server

```json
    "mcp-server-weather":{
      "disables": false,
      "timeout": 60,
      "command": "uv",
      "args":[
        "--directory",
        "D:/code/study/ai/mcp/mcp_server_weather",
        "run",
        "weather.py"
      ],
      "transportType": "stdio"
    }
```

完整的配置文件如下：

```json
{
  "mcpServers": {
    "weather": {
      "timeout": 60,
      "command": "uv",
      "args": [
        "--directory",
        "D:/code/study/ai/mcp/weather",
        "run",
        "weather.py"
      ],
      "type": "stdio",
      "disabled": true
    },
    "fetch": {
      "timeout": 60,
      "command": "uvx",
      "args": [
        "mcp-server-fetch"
      ],
      "type": "stdio"
    },
    "mcp-server-hotnews": {
      "timeout": 60,
      "command": "npx",
      "args": [
        "-y",
        "@wopal/mcp-server-hotnews"
      ],
      "type": "stdio"
    },
    "mcp-server-weather": {
      "timeout": 60,
      "command": "uv",
      "args": [
        "--directory",
        "D:/code/study/ai/mcp/mcp_server_weather",
        "run",
        "weather.py"
      ],
      "type": "stdio"
    }
  }
}
```

这里将之前导入的 weather 暂时禁用



测试：

```
纽约明天的天气怎么样？
```

